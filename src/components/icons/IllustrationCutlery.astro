---
interface Props {
  class?: string;
  width?: number;
  height?: number;
  alt?: string;
}

const { class: className, width, height, alt = 'BipBip cutlery illustration', ...attrs } = Astro.props;
---

<img
  src="/iconografia/Cubiertos 1.svg"
  alt={alt}
  width={width}
  height={height}
  class={className}
  {...attrs}
/>
