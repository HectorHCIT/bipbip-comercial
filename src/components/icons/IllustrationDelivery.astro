---
interface Props {
  class?: string;
  width?: number;
  height?: number;
  alt?: string;
}

const { class: className, width, height, alt = 'BipBip delivery illustration', ...attrs } = Astro.props;
---

<img
  src="/iconografia/renderizado-3d-icono-entrega 1.svg"
  alt={alt}
  width={width}
  height={height}
  class={className}
  {...attrs}
/>
