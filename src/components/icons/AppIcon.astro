---
interface Props {
  class?: string;
  width?: number;
  height?: number;
  alt?: string;
}

const { class: className, width, height, alt = 'BipBip app icon', ...attrs } = Astro.props;
---

<img
  src="/v2.svg"
  alt={alt}
  width={width}
  height={height}
  class={className}
  {...attrs}
/>
